# Configuration values for application.  Don't commit!
DEBUG = True
SECRET_KEY = 'some random secret string'
CLIENT_ID = '<value from portal /client>'
CLIENT_SECRET = '<value from portal /client>'
BASE_URL = 'http://<portal url>/api/'
ACCESS_TOKEN_URL = 'http://<portal url>/oauth/token'
AUTHORIZE_URL = 'http://<portal url>/oauth/authorize'

